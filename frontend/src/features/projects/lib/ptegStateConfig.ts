// Backwards-compat shim — Task 20 deletes this file once all imports migrate to ./stateConfig.
export { STATE_CONFIG as PTEG_STATE_CONFIG, STATE_FALLBACK as PTEG_STATE_FALLBACK } from "./stateConfig";
